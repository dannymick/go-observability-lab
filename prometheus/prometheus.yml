global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "go-api"
    static_configs:
      - targets: ["api:8080"]

  - job_name: "worker"
    static_configs:
      - targets: ["worker:8081"]

# Uncomment for retention configuration
# command:
  # - "--storage.tsdb.retention.time=24h"